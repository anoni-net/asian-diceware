# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Pipeline scratch (regenerated by the build)
data/interim/

# Build outputs are committed for release, but ignore the scratch PDF build dir
output/*.pdf

# Local editor / MCP config (not part of the published project)
.claude/settings.local.json

# macOS junk / build artifact (audit is reproducible via scripts/audit.sh)
.DS_Store
output/audit_report.json
